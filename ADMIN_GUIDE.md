# دليل لوحة تحكم بصريات العادل

## الدخول إلى اللوحة

افتح المسار `/admin` بعد نشر الموقع، ثم سجّل الدخول بكلمة المرور المخزنة في المتغير `ADMIN_PASSWORD`. لا يمكن للزوار تعديل الموقع من دونها.

## ما الذي يمكن تعديله؟

| التبويب | ما يمكنك تغييره |
| --- | --- |
| الرئيسية | العنوان، الوصف، أزرار الواجهة، صورة أو فيديو البطل |
| الخدمات | نصوص ومزايا العدسات والفريمات والنظارات الشمسية |
| التشكيلة | بطاقات المنتجات والصور أو الفيديوهات الخاصة بها |
| العدسات والفريمات | القسم التعريفي والوسيط وقائمة النقاط والزر |
| التواصل | الهاتف، واتساب، الدوام، العنوان، الخريطة، والتذييل |
| التنقل والرسائل | الإعلان العلوي، روابط القائمة، شريط المزايا، والعرض الرئيسي |
| الألوان والهوية | شعار العلامة ورمزها والألوان والخطوط |
| إضافات جديدة | إنشاء أقسام نصية أو مرئية جديدة، إظهارها وإخفاؤها وترتيبها |

## رفع الصور والفيديوهات

يمكنك رفع صور أو فيديوهات حتى 25 ميغابايت من داخل حقل الوسيط. بعد انتهاء الرفع، اضغط **حفظ ونشر** كي يظهر الملف في الموقع. للفيديوهات الكبيرة، يفضل استخدام رابط فيديو مباشر من خدمة استضافة فيديو، ثم لصقه في حقل الرابط.

### إعداد الرفع على Railway

عند نشر الموقع على Railway، أضف متغير البيئة `CLOUDINARY_URL` إلى خدمة الموقع حتى يعمل زر **رفع من الجهاز**. قيمته تكون بصيغة `cloudinary://API_KEY:API_SECRET@CLOUD_NAME`، وتُؤخذ من صفحة **API Keys** في Cloudinary. لا تشارك API Secret في المحادثات أو المستودع.

لاختبار الرفع، افتح `/admin`، ثم ضمن أي حقل صورة اختر **رفع من الجهاز**، واختر صورة صغيرة، وانتظر ظهور المعاينة، ثم اضغط **حفظ ونشر**. افتح الصفحة العامة في نافذة جديدة؛ إذا ظهرت الصورة الجديدة، فالرفع والحفظ يعملان بصورة صحيحة.

### روابط Google Drive والروابط الخارجية

> الرابط الخارجي في الموقع يجب أن يفتح **ملف الصورة أو الفيديو نفسه** مباشرة، لا صفحة معاينة أو صفحة تسجيل دخول.

رابط Drive المعتاد مثل `https://drive.google.com/file/d/FILE_ID/view` هو صفحة معاينة HTML؛ لذلك قد يعمل عند فتحه يدويًا لكنه لا يصلح دائمًا داخل الصورة أو الفيديو في الموقع. المسار الأكثر موثوقية هو اختيار **«رفع من الجهاز»** داخل حقل الوسيط، ثم الضغط على **حفظ ونشر**.

إذا أردت استخدام رابط خارجي، الصق رابطًا مباشرًا ينتهي غالبًا بامتداد مثل `.jpg` أو `.png` أو `.webp` للصورة، أو `.mp4` للفيديو. اختبر الرابط أولًا في نافذة خاصة: يجب أن تظهر الصورة أو يبدأ الفيديو فورًا بلا صفحة وسيطة أو تسجيل دخول. لا تستخدم روابط Drive أو روابط YouTube في حقل الفيديو المباشر.

## النشر والاستضافة

لوحة التحكم تعتمد على تسجيل الدخول وقاعدة البيانات وتخزين الوسائط. لذلك لا تعمل مع حزمة HTML الثابتة القديمة وحدها. انشر النسخة الكاملة الحالية على بيئة تدعم Node.js وقاعدة بيانات MySQL متوافقة، أو انشرها من لوحة المشروع. عند استخدام Railway، انشر كود المشروع الكامل وأضف قاعدة بيانات للخدمة؛ لا ترفع فقط ملف ZIP الثابت السابق.

في إعدادات Railway أضف المتغيرات التالية: `ADMIN_PASSWORD` بكلمة مرور قوية، و`JWT_SECRET` بقيمة عشوائية طويلة، و`DATABASE_URL` لرابط قاعدة بيانات MySQL. بعد النشر، افتح `https://رابط-موقعك/admin` وسجل الدخول باستخدام `ADMIN_PASSWORD`.

> بعد كل تعديل داخل اللوحة، لا تحتاج إلى إعادة بناء الموقع يدويًا؛ يكفي الضغط على **حفظ ونشر**.
